[project]
name = "sleepwise"
version = "0.1.0"
description = "Safety-first supplement guidance engine (sleep) over public NIH/FDA data."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
