# Development dependencies (tests, linting, type-checking). Includes runtime deps.
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient transport
ruff>=0.6
mypy>=1.11
pre-commit>=3.8
