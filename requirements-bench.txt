# Retrieval benchmark only: scripts/benchmark_retrieval.py
#
# Kept out of requirements-dev.txt on purpose. This pulls a full torch stack, and CI
# installs dev requirements on four Python versions per run; paying that download to
# support a script nobody runs in CI would be a poor trade. Install it deliberately:
#
#     pip install -r requirements-bench.txt
sentence-transformers>=5.6.1

# Demo recording: scripts/record_demo.py (also needs: playwright install chromium)
playwright>=1.62.0
pillow>=12.3.0

# Normalizer training: python -m ml.train (inference at runtime is pure Python)
scikit-learn>=1.7.2
