# Runtime dependencies (what the service needs to run).
fastapi>=0.141.1
uvicorn[standard]>=0.52.0
pydantic>=2.13.4
httpx>=0.28.1          # optional LLM + embedding API calls
jinja2>=3.1.6           # server-side templates (shared page shell)
